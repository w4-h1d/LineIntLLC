<section class="container" id="chairman">
    <div class="wrapper flex-row">
        <img src="/images/ceo.jpg" alt="Jakir Hossain, Chairman & CEO" width="400" height="500">
        <div>
            <article class="message-cont">
                <h3>Chairman's Message</h3>
                <p>We are committed to providing quality products, dependable services, and lasting value to every customer. We believe that strong relationships, integrity, and continuous improvement are the foundation of our success. We thank our clients and partners for their trust and look forward to building a strong future together.</p>
            </article>

            <section class="title-cont">
                <h2>Jakir Hossain</h2>
                <p>Chairman&nbsp;&&nbsp;CEO, Line&nbsp;International Project&nbsp;L.L.C.</p>
            </section>
        </div>
    </div>
</section>

<style>
    img {
        max-width: 100%;
        height: auto;
        border-radius: 1rem;
        box-shadow: var(--box-shadow);
    }

    .container {
        padding-inline: 2rem;
        container: ceo / inline-size;

    }

    .wrapper {
        max-width: var(--max-width);
        border-inline: var(--border-dark);
        padding: 6rem 2rem;
        margin: 0 auto;
        gap: clamp(1rem, 4vw, 4rem);
        justify-content: space-evenly;
    }

    .wrapper > div {
        width: 43.5rem;
        max-width: 100%;
        display: flex;
        flex-flow: column nowrap;
        gap: clamp(2rem, 6cqi, 8rem);
    }

    h2, h3 {
        color: var(--primary-200);
    }

    /* .title-cont {
        margin-top: clamp(2rem, 6cqi, 8rem);
    } */

    .title-cont > p {
        font-weight: 600;
        color: var(--primary-200);
        font-size: var(--font-16);
    }

    h3 {
        font-size: var(--font-32);
    }

    .message-cont > p {
        color: var(--black-10);
        line-height: 1.5;
        font-size: var(--font-20);
        margin-top: clamp(0.25rem, 0.5cqi, 0.75rem);
    }

    @container ceo (width < 57rem) {
        .wrapper {
            flex-wrap: wrap;
            justify-content: flex-start;
            gap: clamp(2rem, 4vh, 8rem);
        }

        .wrapper > div {
            flex-flow: column-reverse nowrap;
        }

    }
</style>